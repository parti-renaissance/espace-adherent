{% extends 'base.html.twig' %}

{% block title %}
    {{ parent() }} | Mot de passe oublié
{% endblock %}

{% block banner '' %}

{% block stylesheets %}
    <style type="text/css">
        body {
            height: 100vh;
            justify-content: space-between;
        }
        header,
        footer {
            flex-grow: 0;
        }
        main {
            flex-grow: 1;
        }
    </style>
{% endblock %}

{% block content %}

    <main class="forgot-password">
        <section class="l__wrapper-300">
            <div>
                <h2>Mot de passe oublié</h2>

                <div class="security_password__form">
                    <p class="security__form__title">Saisissez l'adresse e-mail associé à votre compte En Marche, et nous vous enverrons un email afin de réinitialiser votre mot de passe.</p>

                    {{ form_start(form) }}

                        {{ form_errors(form) }}

                        <div class="form__row">
                            {{ form_errors(form.email) }}
                            {{ form_widget(form.email, { attr: {
                                placeholder: 'Addresse e-mail',
                                'aria-label': 'Addresse e-mail',
                                class: 'form--full'
                            } }) }}
                        </div>

                        <div class="form__row">
                            {{ form_widget(form.submit, { label: 'Envoyer un e-mail', attr: { class: 'form form--full btn btn--blue login__button' } }) }}
                        </div>

                        {{ form_rest(form) }}
                    {{ form_end(form) }}
                </div>
            </div>
        </section>
    </main>
{% endblock %}
