{% if object.enabled %}
<a href="{{ path('homepage', {'_switch_user': object.emailAddress}) }}" class="btn btn-sm btn-default view_link"
   title="Impersonnifier" rel="noopener noreferrer">
    <i class="fa fa-user-secret" aria-hidden="true"></i>
    Impersonnifier
</a>
{% endif %}
