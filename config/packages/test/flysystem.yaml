flysystem:
    storages:
        memory.storage:
            adapter: 'memory'
        uploadable_file.storage:
            adapter: 'memory'
        national_event.storage:
            adapter: 'memory'
        public_user_file.storage:
            adapter: 'memory'
