<div class="l__wrapper--aside settings-menu">
    <ul>
        {% set route = app.request.attributes.get('_route') %}
        {% for target, link in {
            'app_user_profile': 'Mon compte',
            'app_user_edit': 'Mes informations personnelles',
            'app_summary_manager_index': 'Modifier mon profil',
            'app_user_set_email_notifications': 'Notifications',
            'app_user_change_password': 'Mot de passe',
        } %}
            <li class="{{ route == target ? 'active' }}">
                <a href="{{ path(target) }}">{{ link }}</a>
            </li>
        {% endfor %}
    </ul>
</div>
