<header class="instance-page__header">
    <div class="l__wrapper instance-page__header__content">
        <div class="text--white instance-page__header__titles">
            <div class="text--uppercase text--small b__nudge--bottom">
                Instance
            </div>
            <div class="instance-page__name">
                <h1 id="territorial-council-name" class="text--large">Comité politique</h1>
            </div>
            <ul class="instance-page__infos">
                <li>
                    {{ include('components/location.html.twig') }}
                    {{ political_committee.name }}
                </li>
                <li>
                    {{ include('components/users.html.twig') }}
                    {{ 'common.members_count'|transchoice(political_committee.memberships.count) }}
                </li>
            </ul>
        </div>
    </div>
</header>
