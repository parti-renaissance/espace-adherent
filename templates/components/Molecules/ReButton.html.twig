<{{ tag }} {{ attributes.defaults({class: this.defaultClass }) }}>
    {% if icon is defined %}
        <twig:Atoms:ReIcon type="{{ icon }}"/>
    {% endif %}

    {{ value }}

    {% if xSyncLoading is defined %}
        <twig:Atoms:ReIcon type="{{ this.loaderType }}" x-show="{{ xSyncLoading }}"/>
    {% endif %}
</{{ tag }}>
