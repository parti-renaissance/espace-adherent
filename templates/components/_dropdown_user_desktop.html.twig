<div class="em-dropdown--container em-nav">
    <div class="l__wrapper">
        <div class="l__row l__row--top">
            <div class="em-nav-dropdown__col">
                <div class="adherent-profile__id l__row b__nudge--bottom">
                    <div class="name b__nudge--right-nano">{{ app.user }}</div>
                    {% if is_certified %}
                        <div class="l__row">
                            {% include('components/_certification_logo.html.twig') %}
                        </div>
                    {% endif %}
                </div>
                <ul class="adherent-profile__roles b__nudge--bottom">
                    {% for role in get_adherent_role_labels(app.user) %}
                        <li>{{ role }}</li>
                    {% endfor %}
                </ul>
                {#                    <div class="adherent-profile__completion b__nudge--bottom">#}
                {#                        <div class="text--body text--small b__nudge--bottom-small">#}
                {#                            <!-- If is not completed -->#}
                {#                            Complétion du profil : 50%#}
                {#                            <!-- If is completed TODO#}
                {#                            👏 Profil complété ! -->#}
                {#                        </div>#}
                {#                        <div class="gauge">#}
                {#                            <span></span>#}
                {#                        </div>#}
                {#                        <div class="b__nudge--top-10">#}
                {#                            <a href="{{ path('app_funnel_general') }}" class="text--body text--small text--blue--dark link--no-decor">Compléter mon profil</a>#}
                {#                        </div>#}
                {#                    </div>#}
                <ul class="">
                    {% if is_granted('ROLE_FORMATION_SPACE') %}
                        <li class="space--5-0"><a href="{{ path('app_formation_home') }}" class="text--body text--small link--no-decor">Formation</a></li>
                    {% endif %}
                    <li class="space--5-0"><a href="https://aide.en-marche.fr" rel="noopener noreferrer" target="_blank" class="text--body text--small link--no-decor">Aide</a></li>
                    {% if is_granted('ROLE_REFERENT') or is_granted('ROLE_COREFERENT') or is_granted('ROLE_DELEGATED_REFERENT') %}
                        <li class="space--5-0"><a href="https://t.me/joinchat/EmY0e1J2fyTv4Fd-cHEMHg">Bug ? Besoin ? Écrivez-nous !</a></li>
                    {% endif %}
                    <li class="space--5-0"><a href="{{ path('logout') }}" class="text--body text--small link--no-decor">Déconnexion</a></li>
                    {% if is_granted('ROLE_PREVIOUS_ADMIN') %}
                        <li class="space--5-0">
                            <a href="{{ path('app_admin_impersonation_exit') }}" class="text--body text--small link--no-decor">
                                Quitter l'impersonnification
                            </a>
                        </li>
                    {% endif %}
                </ul>
            </div>
            <div class="em-nav-dropdown__col">
                <div class="em-nav__menu em-nav__menu--dropdown">
                    <span class="head">Informations</span>
                    <ul class="items">
                        <li><a href="{{ path('app_user_edit') }}">Général</a></li>
                        <li><a href="{{ path('app_certification_request_home') }}">Certification</a></li>
                        <li><a href="{{ path('app_user_change_password') }}">Mot de passe</a></li>
                        <li><a href="{{ path('app_adherent_pin_interests') }}">Centres d'intéret</a></li>
                        <li><a href="{{ path('app_adherent_commitment') }}">Engagement</a></li>
                        <li><a href="{{ path('app_user_set_email_notifications') }}">Communication</a></li>
                    </ul>
                </div>
            </div>
            <div class="em-nav-dropdown__col">
                <div class="em-nav__menu em-nav__menu--dropdown">
                    <span class="head">Activités</span>
                    <ul class="items">
                        {% if is_granted('TERRITORIAL_COUNCIL_MEMBER') %}
                            <li><a href="{{ path('app_adherent_profile_activity', {'_fragment': 'instances'}) }}">Mes instances</a></li>
                        {% endif %}
                        <li><a href="{{ path('app_adherent_profile_activity', {'_fragment': 'committees'}) }}">Mes comités</a></li>
                        <li><a href="{{ path('app_adherent_profile_activity', {'_fragment': 'events'}) }}">Mes événements</a></li>
                        <li><a href="{{ path('app_adherent_profile_activity', {'_fragment': 'citizen-projects'}) }}">Mes projets citoyens</a></li>
                        <li><a href="{{ path('app_adherent_profile_activity', {'_fragment': 'adi'}) }}">Mon atelier des idées</a></li>
                        <li><a href="{{ path('app_adherent_profile_activity', {'_fragment': 'donations'}) }}">Mes dons</a></li>
                    </ul>
                </div>
            </div>
            <div class="em-nav-dropdown__col">
                <div class="em-nav__menu em-nav__menu--dropdown">
                    <span class="head">Documents</span>
                    <ul class="items">
                        <li><a href="{{ path('app_documents_index') }}">Mes documents</a></li>
                    </ul>
                </div>
            </div>
        </div>
    </div>
</div>
