<div {{ attributes.only('class') }}>
    <div class="relative flex-col flex justify-center">
        <div class="justify-start items-start gap-2.5 inline-flex">
            <twig:Atoms:ReCheckbox
                {{ ...attributes.without('class').all() }}
                status="{{ status }}"
                class="mt-1"
                type="radio"
                checked="{{ checked }}"
            />

            {% block content '' %}

            {% if iconToolTip is defined and iconToolTip|length > 0 %}
                <twig:Atoms:ReIcon class="mt-1" type="info-blank" x-tooltip.bottom-end.re="{{ iconToolTip }}"/>
            {% endif %}
        </div>
    </div>
</div>
