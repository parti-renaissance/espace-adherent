<div {{ attributes.defaults({ class: 're-paragraph-status re-paragraph-status--' ~ status ~ (slim ? ' re-paragraph-status--slim') }) }}
    {% if xSyncStatus is defined %}
        :class="'re-paragraph-status--' + {{ xSyncStatus }}"
    {% endif %}>
    {% if icon %}
        <twig:Atoms:ReIcon type="{{ icon }}"/>
    {% endif %}

    <div>{% block content '' %}</div>
</div>
