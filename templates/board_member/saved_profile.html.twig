{% extends 'board_member/layout.html.twig' %}

{% set currentRoute = app.request.attributes.get('_route') %}

{% block content__inner %}
    <h1>Saved profile page</h1>
    <div>
        {# stats #}
    </div>
    <div>
        <h2>{{ results.count }} profil{% if results.count > 1 %}s{% endif %} sauvegardé{% if results.count > 1 %}s{% endif %}</h2>
        {% if results.count > 0 %}
            <a href="{{ path('app_board_member_message_saved_profile') }}">
                Envoyer un message à ces {{ results.count }} personne{% if results.count > 1 %}s{% endif %}
            </a>
        {% endif %}
        {% include('board_member/_list_board_members.html.twig') %}
    </div>
{% endblock %}
