{% if currentRoute is same as('app_board_member_search') %}
    {% if app.user.boardMember.hasSavedBoardMember(member.boardMember)  %}
        <span class="btn btn--smaller btn--light btn-remove-member-list btn--green" data-memberid="{{ member.boardMember.id }}">Profil sauvegardé</span>
    {% else %}
        <span class="btn btn--smaller btn--light btn-add-member-list" data-memberid="{{ member.boardMember.id }}">Sauvegarder ce profil</span>
    {% endif %}
{% elseif currentRoute is same as ('app_board_member_saved_profile') %}
    <span class="btn btn--smaller btn--light btn-remove-member-list btn--green need-to-refresh" data-memberid="{{ member.boardMember.id }}">Supprimer de ma liste</span>
{% endif %}

<a href="{{ path('app_board_member_message_member', {member: member.id}) }}" class="btn btn--smaller btn--light">
    Envoyer un message
</a>
