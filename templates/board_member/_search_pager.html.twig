{% set current_page = (filter.offset / constant('PER_PAGE', filter))|round(0, 'ceil') + 1 %}
{% set last_page = (results.count / constant('PER_PAGE', filter))|round(0, 'ceil') %}

{% if filter.offset > 0 %}
    <a href="{{ path('app_board_member_search') ~ filter.queryStringForOffset(0) }}" class="newbtn newbtn--smaller newbtn--light">
        Première
    </a>
    <a href="{{ path('app_board_member_search') ~ filter.previousPageQueryString }}" class="newbtn newbtn--smaller newbtn--light">
        Précédente
    </a>
{% endif %}

<span class="text--body text--gray text--small">
    Page {{ current_page }} / {{ last_page }}
</span>

{% if filter.offset + constant('PER_PAGE', filter) < results.count %}
    <a href="{{ path('app_board_member_search') ~ filter.nextPageQueryString }}" class="newbtn newbtn--smaller newbtn--light">
        Suivante
    </a>
    <a href="{{ path('app_board_member_search') ~ filter.queryStringForOffset((last_page - 1) * constant('PER_PAGE', filter)) }}" class="newbtn newbtn--smaller newbtn--light">
        Dernière
    </a>
{% endif %}
