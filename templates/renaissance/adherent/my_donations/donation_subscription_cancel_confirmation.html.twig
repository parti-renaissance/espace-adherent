{% extends 'renaissance/adherent/_layout.html.twig' %}

{% block content_right_side %}
<section class="space-y-4 md:space-y-6">
    {{ form_start(confirmation_form) }}
        <p class="text-lg">Êtes-vous sûr(e) de vouloir arrêter votre don mensuel ?</p>
        {{ form_widget(confirmation_form.allow) }}
        {{ form_widget(confirmation_form.deny) }}
    {{ form_end(confirmation_form) }}
</section>
{% endblock %}
