{% extends 'renaissance/adherent/_layout.html.twig' %}

{% block page_title 'Désadhésion' %}

{% block javascripts %}
    {% import 'javascript.js.twig' as js %}

    <script type="text/javascript" src={{ asset('bundles/sonataadmin/vendor/jquery/dist/jquery.min.js') }}></script>
    <script type="text/javascript" src={{ asset('bundles/sonataadmin/vendor/jqueryui/ui/jquery-ui.js') }}></script>
    <script type="text/javascript">
        $(document).ready(function() {
            var anotherReasons = $('#unregistration_reasons_6');
            var toggleComment = function() {
                if (anotherReasons.is(':checked')) {
                    $('#unregistration_another_reason_comment').show();
                } else {
                    $('#unregistration_another_reason_comment').hide();
                }
            };

            // Show/hide comment field
            anotherReasons.change(function() {
                toggleComment();
            });

            toggleComment();
        });
    </script>
{% endblock %}

{% block content_right_side %}

<div>
    <div id="is_adherent" class="settings__unsub">
        <header class="filtered-image progressive-background"
                data-sd="{{ static_asset('em-marcheurs-bg.jpg', { w: 300, q: 20 }) }}"
                data-hd="{{ static_asset('em-marcheurs-bg.jpg', { q: 70 }) }}">
            <div class="l__wrapper--narrow">
                <h1 class="text--center text--larger">
                    Je souhaite supprimer mon adhésion et mon compte <em>En Marche</em>
                </h1>
            </div>
        </header>
        <section class="l__wrapper--600 settings__unsub__intro">
            <p>
                Vous êtes un marcheur, vous avez décidé de vous engager pour
                participer au renouvellement de la vie politique et à la
                transformation de notre pays.
                Grâce à vous, Emmanuel Macron est devenu Président de la
                République et les députés de notre mouvement ont obtenu la
                confiance des Français.<br>
                Notre projet de transformation de la France est en marche.
                Nous vous en remercions du fond du cœur.
            </p>
        </section>

        <section class="l__wrapper--600">
            {{ form_start(form, { attr: { class: 'b__nudge--top-40'} }) }}

            {{ form_errors(form) }}

            <div class="form__row">
                {{ form_label(form.reasons, 'Merci de nous aider à comprendre pourquoi vous souhaitez quitter la communauté des marcheurs en choisissant une ou plusieurs des réponses ci-dessous :') }}
            </div>

            {{ form_errors(form.reasons) }}

            <div class="form l__col">
                <div class="form__checkbox b__nudge--top b__nudge--bottom">
                    {{ form_widget(form.reasons) }}
                </div>
                <div id="unregistration_another_reason_comment">
                    {{ form_errors(form.comment) }}
                    {{ form_widget(form.comment, { attr: { class: 'form__textbox' } }) }}
                </div>
            </div>

            <p class="text--body">
                Merci pour ces précisions.
            </p>
            <p class="text--body">
                Nous vous rappelons que vous pouvez très facilement vous
                <a href="{{ path('app_user_set_email_notifications') }}">
                désabonner de tout ou partie des emails envoyés</a> par le
                mouvement.
            </p>
            <p class="text--body b__nudge--bottom-large">
                Si vous ne souhaitez plus être un marcheur, vous pouvez
                finaliser la suppression de votre adhésion en cliquant ci-dessous.
            </p>

            <div class="text--center">
                <button type="submit" class="btn btn--error">
                    Je confirme la suppression de mon adhésion
                </button>
            </div>

            {{ form_end(form) }}
        </section>

    </div>
</div>

{% endblock %}
