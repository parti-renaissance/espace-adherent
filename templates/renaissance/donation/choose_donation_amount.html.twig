{% extends 'renaissance/donation/_layout.html.twig' %}

{% block back_btn_target path('app_renaissance_homepage') %}

{% block final_javascripts %}
    <script type="text/javascript">
        Bootstrap.onLoad(function () {
            Main.runDonationPage();
        });
    </script>
{% endblock %}

{% block donation_content %}
    <section class="w-96">
        {{ form_start(form) }}
            {{ form_errors(form) }}
            {{ form_rest(form) }}

            <div class="renaissance-donation-widget-wrapper"></div>
        {{ form_end(form) }}

        <div class="or-separator"></div>

        <div class="flex mt-4 justify-between">
            <a href="{{ path('app_renaissance_donation_cheque') }}" class="w-full py-5 bg-dark-blue hover:bg-blue-500 text-white hover:text-white px-4 border border-blue-500 hover:border-transparent rounded font-din font-bold uppercase text-center">
                Je donne par chèque
            </a>
        </div>
    </section>
{% endblock %}
