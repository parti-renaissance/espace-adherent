{% extends 'SonataAdminBundle:CRUD:list.html.twig' %}

{% block list_footer %}
    <div class="container-fluid" style="padding: 10px 0">
        <div class="row">
            <div class="col-md-1 col-md-offset-8">
                <a href="{{ path('admin_city_card_export_all') }}" class="btn btn-success">Export Villes</a>
            </div>
            <div class="col-md-1">
                <a href="{{ path('admin_city_card_export_lists') }}" class="btn btn-success">Exporter Listes</a>
            </div>
        </div>
    </div>
{% endblock %}
