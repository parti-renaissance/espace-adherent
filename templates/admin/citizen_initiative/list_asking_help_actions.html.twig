{% extends 'SonataAdminBundle:CRUD:base_list_field.html.twig' %}

{% block field %}
    <div style="width: 160px;">
        <a href="{{ path("app_citizen_initiative_expert_assistance", {'slug': object.slug}) }}" class="btn btn-xs btn-default" title="Voir sur le site">
            <i class="fa fa-search-plus" aria-hidden="true"></i>
            Voir sur le site
        </a>

        <a href="{{ path('admin_app_asking_help_citizeninitiative_edit', {id: object.id}) }}" class="btn btn-xs btn-default" title="Éditer">
            <i class="fa fa-pencil" aria-hidden="true"></i>
            Éditer
        </a>
    </div>
{% endblock %}
