{% extends 'SonataAdminBundle:CRUD:base_list_field.html.twig' %}

{% block field %}
    {% set titles = object.title %}

    {% if titles.fr is defined %}
        {{ titles.fr }}
    {% endif %}

    {% if titles.en is defined %}
        <br>
        {{ titles.en }}
    {% endif %}
{% endblock %}
