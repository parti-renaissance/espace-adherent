{% extends 'admin/layout.html.twig' %}

{% block javascripts %}
    {{ parent() }}
    <script src="https://cdn.datatables.net/1.13.7/js/jquery.dataTables.min.js"></script>
    <script src="https://cdn.datatables.net/1.13.7/js/dataTables.bootstrap.min.js"></script>

    <script>
        $(document).ready(function() {
            $('.dataTable').DataTable({
                language: {
                    url: "//cdn.datatables.net/plug-ins/1.10.20/i18n/French.json"
                },
                searching: false,
                lengthChange: false,
                paging: false,
            });
        });
    </script>
{% endblock %}

{% block stylesheets %}
    {{ parent() }}
    <link rel="stylesheet" href="https://cdn.datatables.net/1.13.7/css/dataTables.bootstrap.min.css" />
{% endblock %}

{% block sonata_page_content_header %}
    <h1>
        Adhésion par département pour l'année <strong>{{ "now"|date("Y") }}</strong>
    </h1>
{% endblock %}

{% block sonata_admin_content %}
    {% set first = stats_per_department|first %}

    <div class="row">
        <div class="col-md-3 col-xs-12">
            <div class="info-box">
                <span class="info-box-icon bg-green"><i class="fa fa-users"></i></span>
                <div class="info-box-content">
                    <span class="info-box-text">À jour {{ "now"|date("Y") }}</span>
                    <span class="info-box-number">{{ first['À jour N'] }}</span>
                </div>
            </div>
        </div>

        {% set percentPrimo = first['À jour N'] > 0 ? (first['Primo adhésion'] * 100.0 / first['À jour N']) : 0 %}
        <div class="col-md-3 col-xs-12">
            <div class="info-box bg-aqua">
                <span class="info-box-icon"><i class="fa fa-child"></i></span>
                <div class="info-box-content">
                    <span class="info-box-text">Primo adhésion</span>
                    <span class="info-box-number">{{ first['Primo adhésion'] }}</span>
                    <div class="progress">
                        <div class="progress-bar" style="width: {{ percentPrimo }}%"></div>
                    </div>
                    <span class="progress-description">
                        {{ percentPrimo|number_format(1, ',', ' ')|replace({',0': ''}) }} %
                    </span>
                </div>
            </div>
        </div>

        {% set percentReadhesion = first['À jour N'] > 0 ? (first['Réadhésion'] * 100.0 / first['À jour N']) : 0 %}
        <div class="col-md-3 col-xs-12">
            <div class="info-box bg-yellow">
                <span class="info-box-icon"><i class="fa fa-heartbeat"></i></span>
                <div class="info-box-content">
                    <span class="info-box-text">Réadhésion</span>
                    <span class="info-box-number">{{ first['Réadhésion'] }}</span>
                    <div class="progress">
                        <div class="progress-bar" style="width: {{ percentReadhesion }}%"></div>
                    </div>
                    <span class="progress-description">
                        {{ percentReadhesion|number_format(1, ',', ' ')|replace({',0': ''}) }} %
                    </span>
                </div>
            </div>
        </div>

        {% set percentElu = first['À jour N'] > 0 ? (first['À jour de cotisation élu'] * 100.0 / first['À jour N']) : 0 %}
        <div class="col-md-3 col-xs-12">
            <div class="info-box bg-green">
                <span class="info-box-icon"><i class="fa fa-thumbs-o-up"></i></span>
                <div class="info-box-content">
                    <span class="info-box-text">À jour de cotisation élu</span>
                    <span class="info-box-number">{{ first['À jour de cotisation élu'] }}</span>
                    <div class="progress">
                        <div class="progress-bar" style="width: {{ percentElu }}%"></div>
                    </div>
                    <span class="progress-description">
                        {{ percentElu|number_format(1, ',', ' ')|replace({',0': ''}) }} %
                    </span>
                </div>
            </div>
        </div>
    </div>

    <div class="row">
        <div class="col-xs-12">
            <div class="box">
                <div class="box-body">
                    <div class="dataTables_wrapper form-inline dt-bootstrap">
                        <div class="row">
                            <div class="col-sm-12">
                                <table class="table table-bordered table-hover dataTable">
                                    <thead>
                                        <tr role="row">
                                            {% for key in first|keys %}
                                                <th class="sorting">{{ key }}</th>
                                            {% endfor %}
                                        </tr>
                                    </thead>

                                    <tbody>
                                        {% for stats in stats_per_department[1:] %}
                                            <tr>
                                                {% for value in stats %}
                                                    <td class="{{ value == '0' ? 'text-muted' }}" style="text-wrap: nowrap;">{{ value }}</td>
                                                {% endfor %}
                                            </tr>
                                        {% endfor %}
                                    </tbody>
                                </table>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
{% endblock %}
