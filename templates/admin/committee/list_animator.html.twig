{% extends '@SonataAdmin/CRUD/base_list_field.html.twig' %}

{% block field %}
    {% set animator = object.animator %}

    {% if animator %}
        {{ include(('admin/adherent/display_fullname_certified.html.twig'), {object: animator}) }}
    {% endif %}
{% endblock %}
