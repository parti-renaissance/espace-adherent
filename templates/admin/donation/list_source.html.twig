{% extends '@SonataAdmin/CRUD/base_list_field.html.twig' %}

{% block field %}
    {% if object.isForMembership %}
        <span class="badge bg-blue">
            {{ ('donation.source.membership')|trans }}
        </span>
    {% endif %}
{% endblock %}
