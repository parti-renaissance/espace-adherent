{% extends 'SonataAdminBundle:CRUD:base_list_field.html.twig' %}

{% block field %}
    {% if object.isDraft %}
        -
    {% else %}
        {{ object.daysBeforeDeadline }} jour{{ 1 != object.daysBeforeDeadline ? 's' : '' }}
    {% endif %}
{% endblock %}
