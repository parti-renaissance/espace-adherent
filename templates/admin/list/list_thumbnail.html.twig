{% extends get_admin_template('base_list_field', admin.code) %}

{% block field %}
    <div style="text-align: center;">
        {% if object.hasYoutubeThumbnail %}
            <img src="{{ object.getYoutubeThumbnail }}" style="max-height: 70px; max-width: 100px;" />
        {% endif %}
    </div>
{% endblock %}
