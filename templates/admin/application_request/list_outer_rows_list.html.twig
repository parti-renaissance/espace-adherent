{% for data in admin.datagrid.results %}
    {% set object = data.0 %}
    {% do object.setIsAdherent(data.is_adherent) %}

    <tr>
        {% include get_admin_template('inner_list_row', admin.code) %}
    </tr>
{% endfor %}
