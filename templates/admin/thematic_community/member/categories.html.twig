{% extends 'SonataAdminBundle:CRUD:base_list_field.html.twig' %}

{% block field %}
    {% for category in object.userListDefinitions %}
        <span class="label" style="background: {{ category.color }}">{{ category }}</span>
    {% endfor %}
{% endblock %}
