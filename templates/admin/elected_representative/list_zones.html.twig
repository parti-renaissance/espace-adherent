{% extends 'SonataAdminBundle:CRUD:base_list_field.html.twig' %}

{% block field %}
    {% for mandate in object.currentMandates %}
        {{ mandate.zone }} {% if not loop.last %}<br />{% endif %}
    {% endfor %}
{% endblock %}
