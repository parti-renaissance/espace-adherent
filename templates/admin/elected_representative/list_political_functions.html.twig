{% extends 'SonataAdminBundle:CRUD:base_list_field.html.twig' %}

{% block field %}
    {% for politicalFunction in object.currentPoliticalFunctions %}
        {{ politicalFunction }} {% if not loop.last %}<br />{% endif %}
    {% endfor %}
{% endblock %}
