{% extends '@SonataAdmin/CRUD/base_list_field.html.twig' %}

{% block field %}
    <span class="badge
        {% if object.isAdd %}
            bg-green
        {% elseif object.isRemove %}
            bg-red
        {% endif %}
        ">
        {{ ('team_member_history.action.'~value)|trans }}
    </span>
{% endblock %}
