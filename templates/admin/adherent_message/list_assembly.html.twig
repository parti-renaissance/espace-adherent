{% extends '@SonataAdmin/CRUD/base_list_field.html.twig' %}

{% block field %}
    {% set filter = object.filter %}

    {% if filter %}
        {% set assembly = filter.getAssemblyZone() %}

        {% if assembly %}
            {{ assembly }}
        {% endif %}
    {% endif %}
{% endblock %}
