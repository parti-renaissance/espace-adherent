<script type="text/javascript">
    const genderField = dom("{{ genderField }}");
    const customGenderField = dom("{{ customGenderField }}");

    const checkGender = () => {
        if ('other' === genderField.value) {
            customGenderField.required = true;
            removeClass(customGenderField.parentElement, 'invisible');
        } else {
            addClass(customGenderField.parentElement, 'invisible');
            customGenderField.required = false;
            customGenderField.value = '';
        }
    };

    on(genderField, 'change', checkGender);
    checkGender();

</script>
