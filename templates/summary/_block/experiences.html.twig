<section class="l__wrapper--medium">
    {% block summary_experiences %}
        <div class="l__row l__row--h-stretch l__row--top l__tablet--col b__nudge--bottom-large">
            <h2>Expériences</h2>
            <div id="summary_experiences">
                {% for experience in summary.experiences %}
                    <div>
                        <h3>{{ experience.position }}</h3>
                        <h4>{{ experience.company|upper }}</h4>
                        <span>{{ experience.startedAt|localizeddate('none', 'none', 'fr_FR', null, 'MMMM Y')|capitalize }} - {{ experience.endedAt|localizeddate('none', 'none', 'fr_FR', null, 'MMMM Y')|capitalize }} ( todo )</span>
                        <br />
                        <p>{{ experience.description }}</p>
                        {% block modify %}{% endblock %}
                    </div>
                {% endfor %}
            </div>
        </div>
    {% endblock %}
</section>
