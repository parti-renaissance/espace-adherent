<div class="tonmacron__header">
    <h1 class="text--large b__nudge--top-large text--center l__wrapper--slim">
        Pourquoi voter pour le ou la candidat(e) <em>La République En Marche !</em> aux législatives
    </h1>
</div>

<section class="tonmacron__content l__wrapper l__mobile--col space--60-0">
    <div>
        <ul class="tonmacron__steps">
            <li></li>
            <li></li>
            <li class="active"></li>
            <li></li>
            <li></li>
        </ul>

        {{ form_start(invitation_form) }}
        {{ form_errors(invitation_form) }}

            <h5 class="tonmacron__title">
                Sélectionnez les deux thèmes qui intéressent le plus {{ invitation.friendFirstName }}
            </h5>

            <div class="form l__row l__row--wrap">
                {% for checkbox in invitation_form.friendInterests %}
                    <div class="form__checkbox form__checkbox--col">
                        {{ form_widget(checkbox) }}
                        {{ form_label(checkbox) }}
                    </div>
                {% endfor %}
            </div>

            <div class="text--center b__nudge--top-large">
                <a href="{{ path('app_ton_macron_invite_restart') }}" class="btn">Recommencer</a>
                {{ form_widget(invitation_form.fill_interests, {'attr': {'class': 'btn btn--blue'}}) }}
            </div>

        {{ form_end(invitation_form) }}
    </div>
</section>
