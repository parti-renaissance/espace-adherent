{% extends 'procuration/_layout_thanks.html.twig' %}

{% block page_title 'Procuration - Demande (Mandant)' %}

{% set copylink = url('app_procuration_request', {slug: procuration_request.rounds.first.election.slug}) %}

{% block thanks_text %}
    {{ procuration_request.rounds.first.election.requestConfirmation|replace({
        '{{ email }}': procuration_request.email,
        '{{email}}': procuration_request.email,
    })|markdown_to_html }}
{% endblock %}
