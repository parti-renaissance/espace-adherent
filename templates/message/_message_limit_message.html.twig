{% set message_limit = get_message_limit(message_type) %}

{% if message_limit %}
    <p>
        Attention, le nombre de mails envoyés par mois est limité à {{ message_limit }} pour les {{ ('adherent_message.limit.' ~ message_type)|trans }}.
        Vous en avez déjà envoyé {{ get_sent_message_count(app.request, app.user, message_type) }} ce mois-ci.
    </p>
{% endif %}
