{% apply spaceless %}
{% set homepage_url = 'https://'.renaissance_host %}
{% set is_previous_admin = is_granted('ROLE_PREVIOUS_ADMIN') %}
{% endapply %}<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <title>{% block title %}{% apply spaceless %}{% block page_title '' %}{% endapply %} | {% block app_title 'Renaissance' %}{% endblock %}</title>

    {% block favicons %}
        <link rel="icon" type="image/png" href="{{ asset('favicon-re.png') }}" sizes="32x32" />
    {% endblock %}

    <meta name="viewport" content="user-scalable=no, initial-scale=1, maximum-scale=1, minimum-scale=1, width=device-width" />

    {% if enable_facebook_pixel %}
        <meta name="facebook-domain-verification" content="gebt3ezpkea15abp02jgpfdkey39oj" />
        <!-- Meta Pixel Code -->
        <script>
            !function(f,b,e,v,n,t,s)
            {if(f.fbq)return;n=f.fbq=function(){n.callMethod?
                n.callMethod.apply(n,arguments):n.queue.push(arguments)};
                if(!f._fbq)f._fbq=n;n.push=n;n.loaded=!0;n.version='2.0';
                n.queue=[];t=b.createElement(e);t.async=!0;
                t.src=v;s=b.getElementsByTagName(e)[0];
                s.parentNode.insertBefore(t,s)}(window, document,'script',
                'https://connect.facebook.net/en_US/fbevents.js');
            fbq('init', '1625401927875774');
            fbq('track', 'PageView');
        </script>
        <noscript>
            <img height="1" width="1" style="display:none" src="https://www.facebook.com/tr?id=1625401927875774&ev=PageView&noscript=1"/>
        </noscript>
        <!-- End Meta Pixel Code -->
    {% endif %}

    <link rel="canonical" href="{% block canonical_url homepage_url %}" />
    <link rel="stylesheet" type="text/css" href="{{ webpack_asset('bootstrap.css') }}" />
    <link rel="stylesheet" type="text/css" href="{{ asset('css/fontawesome-v4.min.css') }}" />

    {% block stylesheets '' %}
    {% block head_javascripts '' %}
</head>
<body class="font-maax antialiased text-gray-600 {{ block('body_classes') ?? '' }}">
    {{ include('components/_re-sprite.svg.twig') }}
    {% block flash_message %}
        {% for label, messages in app.flashes(['error', 'info', 'success']) %}
            {% if messages|length %}
                {% if label == 'error' %}
                    <div class="p-4 text-sm text-red-700 bg-red-100 dark:bg-red-200 dark:text-red-800" role="alert">
                {% else %}
                    <div class="p-4 text-sm text-blue-700 bg-blue-100 dark:bg-blue-200 dark:text-blue-800" role="alert">
                {% endif %}
                    {% for message in messages %}
                        <div class="flash--{{ label }}">{{ message|trans }}</div>
                    {% endfor %}
                </div>
            {% endif %}
        {% endfor %}
    {% endblock %}

    {% block banner '' %}
    {% block header '' %}
    {% block content '' %}
    {% block footer '' %}

    {% set session_modal_context = get_session_modal_context(app.request) %}

    {% if session_modal_context %}
        {% include 'components/_adherent_session_modal.html.twig' %}
    {% endif %}

    <div id="modal-wrapper"></div>

    <script type="text/javascript" src="{{ webpack_asset('bootstrap.js') }}"></script>

    {% block javascripts '' %}

    <script type="text/javascript">
        on(window, 'load', function() {
            Bootstrap.boot(
                '{{ app_version }}',
                {{ sentry_dsn ? ('\''~sentry_dsn~'\'')|raw : 'null' }},
                '{{ app_environment }}',
                '{{ app.user ? app.user.getUsername() }}'
            );
        });
    </script>

    {% if axeptio_clid %}
        <script>
            window.axeptioSettings = {
                clientId: "{{ axeptio_clid }}",
                cookiesVersion: "renaissance-fr",
            };

            (function(d, s) {
                var t = d.getElementsByTagName(s)[0], e = d.createElement(s);
                e.async = true; e.src = "//static.axept.io/sdk.js";
                t.parentNode.insertBefore(e, t);
            })(document, "script");
        </script>
    {% endif%}

    {% block final_javascripts '' %}

    <script>
        Bootstrap.onLoad(() => {
            window.Alpine.start();
        });
    </script>
</body>
</html>
