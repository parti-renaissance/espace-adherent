{{ introduction.content|replace({'%friend_first_name%': invitation.friendFirstName|e})|raw }}

{% apply spaceless %}
    <ul>
        {% if gender_choice %}
            <li>{{ gender_choice.content|raw }}</li>
        {% endif %}
        {% for argument in invitation.arguments %}
            <li>{{- argument|raw }}</li>
        {% endfor %}
    </ul>
{% endapply %}

{{ conclusion.content|raw }}
