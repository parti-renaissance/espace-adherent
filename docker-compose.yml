version: "2"

services:
    ## en-marche.fr
    ###############
    app:
        networks:
          default:
            aliases:
              - enmarche.dev
              - enmarche.code
              - enmarche.local
        build:
            context: docker/dev
        depends_on:
            - db
            - redis
            - rabbitmq
        volumes:
            - .:/app

    db:
        image: mysql:5.7
        environment:
            MYSQL_ROOT_PASSWORD: root

    redis:
        image: redis:3.2

    ## common
    ####################
    rabbitmq:
        image: rabbitmq:3-management
