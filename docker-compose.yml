version: "2"

services:
    ## en-marche.fr
    ###############
    app:
        networks:
          default:
            aliases:
              - enmarche.dev
              - enmarche.code
              - enmarche.local
        build:
            context: docker/dev
        depends_on:
            - enmarche_db
            - enmarche_redis
            - rabbitmq
        volumes:
            - .:/app

    enmarche_db:
        image: mysql:5.7
        environment:
            MYSQL_ROOT_PASSWORD: root

    enmarche_redis:
        image: redis:3.2

    ## common
    ####################
    rabbitmq:
        image: rabbitmq:3-management
